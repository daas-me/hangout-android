<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme -->
    <style name="Theme.HangOut" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_main</item>
        <item name="colorPrimaryVariant">@color/purple_dark</item>
        <item name="colorAccent">@color/purple_main</item>
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
        <item name="colorSurface">@color/card_bg</item>
        <item name="android:colorBackground">@color/bg_dark</item>
        <item name="colorBackgroundFloating">@color/card_bg</item>
    </style>

    <!-- TextInputLayout — dark outlined style matching the web app inputs -->
    <style name="HangOutInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundColor">@color/input_bg</item>
        <item name="boxStrokeColor">@color/input_border_state</item>  <!-- accepts ColorStateList -->
        <item name="boxStrokeWidth">1.5dp</item>
        <item name="boxStrokeWidthFocused">2dp</item>
        <item name="boxCornerRadiusTopStart">10dp</item>
        <item name="boxCornerRadiusTopEnd">10dp</item>
        <item name="boxCornerRadiusBottomStart">10dp</item>
        <item name="boxCornerRadiusBottomEnd">10dp</item>
        <item name="hintTextColor">@color/text_muted</item>
        <item name="android:textColorHint">@color/text_muted</item>
        <item name="boxStrokeErrorColor">@color/red_accent</item>
        <item name="errorTextColor">@color/red_accent</item>
        <item name="passwordToggleTint">@color/text_muted</item>
        <item name="android:editTextColor">@color/text_primary</item>
    </style>

    <!-- Disabled variant for email field in Edit Profile -->
    <style name="HangOutInputLayoutDisabled" parent="HangOutInputLayout">
        <item name="boxStrokeColor">@color/divider</item>
        <item name="boxBackgroundColor">#0DFFFFFF</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.MaterialComponents.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppBottomSheetStyle</item>
    </style>

    <style name="AppBottomSheetStyle" parent="Widget.MaterialComponents.BottomSheet">
        <item name="android:background">@android:color/transparent</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="android:elevation">0dp</item>
        <item name="elevation">0dp</item>
    </style>

    <style name="DarkEditText" parent="Widget.MaterialComponents.TextInputEditText.OutlinedBox">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textColorHint">@color/text_muted</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="DarkInputTheme">
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="android:textColorHint">@color/text_muted</item>
        <item name="hintTextColor">@color/text_muted</item>
    </style>

    <!-- Spinner style for dark backgrounds -->
    <style name="HangOutSpinner" parent="Widget.AppCompat.Spinner">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textColorHint">@color/text_muted</item>
        <item name="android:background">@drawable/spinner_bg</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- Chat pill input — rounded, dark fill, purple focus stroke -->
    <style name="HangOutChatInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundColor">#1A1A2E</item>
        <item name="boxStrokeColor">@color/input_stroke_selector</item>
        <item name="boxStrokeWidth">1.5dp</item>
        <item name="boxStrokeWidthFocused">2dp</item>
        <item name="boxCornerRadiusTopStart">24dp</item>
        <item name="boxCornerRadiusTopEnd">24dp</item>
        <item name="boxCornerRadiusBottomStart">24dp</item>
        <item name="boxCornerRadiusBottomEnd">24dp</item>
        <item name="hintTextColor">@color/text_muted</item>
        <item name="android:textColorHint">@color/text_muted</item>
    </style>

    <!-- Custom AlertDialog theme for HangOut -->
    <style name="HangOutAlertDialogTheme" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <!-- Dialog background -->
        <item name="android:windowBackground">@drawable/dialog_bg</item>
        <item name="android:colorBackground">@color/card_bg</item>
        <item name="colorSurface">@color/card_bg</item>
        <!-- Text colors -->
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_muted</item>
        <!-- Button colors -->
        <item name="buttonBarPositiveButtonStyle">@style/HangOutAlertDialogButton.Positive</item>
        <item name="buttonBarNegativeButtonStyle">@style/HangOutAlertDialogButton.Negative</item>
        <item name="buttonBarNeutralButtonStyle">@style/HangOutAlertDialogButton.Neutral</item>
    </style>

    <!-- Positive button — purple -->
    <style name="HangOutAlertDialogButton.Positive" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/purple_light</item>
    </style>

    <!-- Negative/Neutral buttons — muted text -->
    <style name="HangOutAlertDialogButton.Negative" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/text_muted</item>
    </style>

    <style name="HangOutAlertDialogButton.Neutral" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/text_muted</item>
    </style>

    <!-- Popup menu theme for dropdowns and popups -->
    <style name="HangOutPopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@color/card_bg</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <!-- Theme overlay for popups and dropdowns -->
    <style name="HangOutPopupTheme" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:popupMenuStyle">@style/HangOutPopupMenuStyle</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_muted</item>
        <item name="android:colorBackground">@color/card_bg</item>
        <item name="colorBackgroundFloating">@color/card_bg</item>
    </style>

    <!-- AutoCompleteTextView dropdown style -->
    <style name="HangOutAutoCompleteStyle" parent="Widget.AppCompat.AutoCompleteTextView">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textColorHint">@color/text_muted</item>
        <item name="android:background">@color/input_bg</item>
    </style>

</resources>